# Project Lantern Currentness Status V1

Status: SOURCE-LEVEL FAIL-CLOSED CURRENTNESS GUARD

## Current backend state

WoWSQL is retired from Project Lantern currentness under current live project authority.

This repository does not currently establish a replacement live Lantern runtime.

Therefore:

`LANTERN_CURRENTNESS = UNKNOWN`

## Successor source

Build Team Two PR #49 (`thebrazenbeard/bt2`) is the current source candidate for a provider-neutral PostgreSQL/SQL Connectome V4 Lantern runtime contract.

That source candidate is not an installed Project Lantern runtime merely because it exists or passes source/database qualification.

Until the replacement PostgreSQL runtime is reconstructed and V4 runtime acceptance passes, this repository must not claim current Lantern material.

## No fallback rule

Do not fall back to:

- WoWSQL;
- Supabase;
- another unqualified database/provider;
- Git repository content;
- Project prose;
- benchmark fixtures;
- ChatGPT conversation history;
- model memory or inference

as current Lantern runtime state.

Historical material may still be used as historical evidence when explicitly identified as such.

## State separation

Report these separately:

- source package readiness;
- database reconstruction;
- runtime qualification;
- Project installation;
- fresh-chat behavioral consumption;
- protected effects.

A PASS in one state does not imply PASS in another.

## Authority ceiling

This status file does not authorize:

- installing a replacement runtime;
- changing Project instructions/files;
- enabling producer authority;
- appending governed material;
- merging or deploying source;
- changing credentials or permissions;
- deleting historical providers or evidence.

Those remain separately authorized effects.

## Recovery rule

If a request requires current Lantern material before a qualified successor runtime is available, return `UNKNOWN` for Lantern-dependent currentness and continue only with evidence sources that are explicitly valid for the remaining task.

Do not silently substitute another source as Lantern currentness.
